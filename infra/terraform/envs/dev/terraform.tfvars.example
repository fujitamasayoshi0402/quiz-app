# cp terraform.tfvars.example terraform.tfvars で写して値を入れる。terraform.tfvars は Git の管理外

# Route 53 に登録済みのドメイン（ホストゾーンの名前）
domain_name = "example.com"

# ECR に push したイメージのタグ（git rev-parse --short=12 HEAD）
quiz_service_image_tag = "0123456789ab"
